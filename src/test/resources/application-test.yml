spring:
  datasource:
    url: jdbc:h2:mem:test_mem
    driver-class-name: org.h2.Driver

  jpa:
    properties.hibernate:
      hdm2.auto: update
      show_sql: true
      format_sql: true

test:
  email: '@test-email.com'